    pipeline {
        // tool { 'nodejs' }
        agent {any 'any'}
        stages {

            stage ('SCM') { 
                steps {
                    checkout scm
                }
            }

            stage ('Build Docker Image') {
                steps {
                    sh "docker build -t test ."
                }

            }
        }
    }
